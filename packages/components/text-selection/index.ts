import { withInstall } from "@wxp-ui/utils/with-install"
import TextSelection from './src/text-selection.vue'

const MTextSelection = withInstall(TextSelection)

export {
  MTextSelection
}

export default MTextSelection
